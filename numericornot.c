#include<stdio.h> 
#include<stdlib.h> 
int main() 
{ 
    char str[100];
    int i; 
    gets(str);
   if(isdigit(str[i]) != 0) 
            {
            printf("yes");
            } 
    else
    {
    printf("No");
    }
} 
